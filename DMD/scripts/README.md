# DMD scripts

Future command-line entry points for preprocessing, windowed DMD estimation, mode tracking, validation, and reporting belong here.

Keep reusable implementation code in a future `DMD/src/` package and tests in `DMD/tests/`; scripts should remain thin, configuration-driven entry points.
